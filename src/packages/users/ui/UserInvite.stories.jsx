import React from 'react';

export default {
  title: 'packages/users/Invite Form',
};

export const initialState = () => (
  <div>TODO: UserInvite story</div>
);

initialState.story = {
  name: 'Initial state',
};
