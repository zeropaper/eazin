import React from 'react';

import { View as ViewPropTypes } from 'eazin-ui/src/core/plugins.propTypes';

const SettingsView = () => (
  <div>
    Settings View
  </div>
);
SettingsView.propTypes = ViewPropTypes;

export default SettingsView;
