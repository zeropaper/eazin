import React from 'react';

import { View as ViewPropTypes } from '../../core/plugins.propTypes';

const BView = () => (
  <div>
    View
  </div>
);

BView.propTypes = ViewPropTypes;

export default BView;
