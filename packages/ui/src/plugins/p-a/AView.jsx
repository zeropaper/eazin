import React from 'react';

import { View as ViewPropTypes } from '../../core/plugins.propTypes';

const AView = () => (
  <div>
    View
  </div>
);

AView.propTypes = ViewPropTypes;

export default AView;
